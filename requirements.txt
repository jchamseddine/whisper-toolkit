# Transcription locale — macOS Apple Silicon uniquement.
# Sur une autre plateforme, cette ligne fait échouer `pip install -r requirements.txt` :
# le marqueur d'environnement ci-dessous la neutralise hors Mac ARM.
mlx-whisper; sys_platform == "darwin" and platform_machine == "arm64"

# Diarisation (identification des locuteurs)
whisperx

# Lecture du token Hugging Face depuis .env (requis par src/diarize.py)
python-dotenv

# Téléchargement audio depuis YouTube & co.
yt-dlp

# Résumé via l'API Claude (requiert ANTHROPIC_API_KEY dans .env)
anthropic

# Interface web (app.py). Le CLI n'en dépend pas : `python src/cli.py` marche
# sans ce paquet.
streamlit

# Fenêtre native pour `launch_desktop.py`, qui affiche l'interface Streamlit
# sans passer par un navigateur. Ni le CLI ni `streamlit run app.py` n'en
# dépendent. Sur macOS, tire PyObjC (WebKit) avec lui.
pywebview
